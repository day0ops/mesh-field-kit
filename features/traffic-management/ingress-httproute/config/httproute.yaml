# Reference schema only — this file is not loaded at runtime.
# The HTTPRoute resource is built in-memory by IngressHttpRouteFeature.buildHttpRoute().
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httproute
  namespace: default
spec:
  parentRefs:
    - name: gateway
      namespace: default
  hostnames:
    - example.com
  rules: []
