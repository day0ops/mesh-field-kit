apiVersion: mesh.demo/v1
kind: UseCase
metadata:
  name: global-service-discovery-with-kgateway
  description: |
    Demonstrate cross-cluster service discovery using global service scope.
    - Services labeled solo.io/service-scope=global get a unified mesh.internal hostname
    - Annotate with networking.istio.io/traffic-distribution: Any to distribute across clusters
    - Endpoints from both clusters are merged under the same global hostname
    - Uses kgateway for ingress traffic to productpage on east cluster

spec:
  diagram: |
    flowchart LR
      Client -->|HTTP| KGW[kgateway\nkgateway-system/east]
      KGW -->|HTTPRoute| PP[productpage\nbookinfo/east]
      PP -->|reviews.bookinfo.mesh.internal| ZTe[ztunnel east]
      ZTe -->|HBONE/mTLS| EWG[east-west gw]
      EWG --> ZTw[ztunnel west]
      ZTw --> R[reviews west]

  infra: eks-multi-cluster

  requires:
    applications:
      - name: bookinfo
        namespace: bookinfo
        clusters:
          - name: west
          - name: east

  features:
    - name: global-service
      description: Make bookinfo services globally accessible
      config:
        namespace: bookinfo
        services:
          - name: productpage
            scope: global
          - name: reviews
            scope: global
            annotations:
              networking.istio.io/traffic-distribution: Any
        deploymentPatches:
          - deployment: productpage-v1
            env:
              REVIEWS_HOSTNAME: reviews.bookinfo.mesh.internal
      clusters:
        - name: west
        - name: east

    - name: ingress-httproute
      description: Route external traffic to productpage
      config:
        gatewayName: bookinfo-gateway
        gatewayNamespace: kgateway-system
        namespace: bookinfo
        hostname: "{{env.domains.app}}"
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - name: productpage
                port: 9080
      clusters:
        - name: east

  tests:
    - name: global-service-entries-exist
      description: Verify global ServiceEntry resources are created for bookinfo services on both clusters
      steps:
        - action: exec
          clusters:
            - name: west
          command: kubectl get serviceentry -n istio-system -o jsonpath='{.items[*].spec.hosts[*]}'
          timeout: 20s
        - action: verify
          expect:
            contains:
              - productpage.bookinfo.mesh.internal
              - reviews.bookinfo.mesh.internal
        - action: exec
          clusters:
            - name: east
          command: kubectl get serviceentry -n istio-system -o jsonpath='{.items[*].spec.hosts[*]}'
          timeout: 20s
        - action: verify
          expect:
            contains:
              - productpage.bookinfo.mesh.internal
              - reviews.bookinfo.mesh.internal

    - name: reviews-global-endpoints
      description: Verify istioctl zc shows cross-cluster autogen WorkloadEntry for reviews on east
      clusters:
        - name: east
      steps:
        - action: istioctl-zc-endpoints
          hostname: reviews.bookinfo.mesh.internal
          timeout: 30s
          retries: 5
          retryDelay: 6000
        - action: verify
          expect:
            contains:
              - autogen.west.bookinfo.reviews
              - Healthy

    - name: productpage-accessible
      description: Verify productpage is reachable via kgateway ingress
      clusters:
        - name: east
      steps:
        - action: send-request
          hostname: "{{env.domains.app}}"
          autoDetectGwAddress: true
          path: /productpage
          timeout: 30s
          retries: 5
          retryDelay: 6000
        - action: verify
          expect:
            statusCode: 200

    # - name: cross-cluster-global-service
    #   description: Verify reviews on east is reachable from productpage on west via mesh.internal hostname
    #   clusters:
    #     - name: west
    #   steps:
    #     - action: exec
    #       command: kubectl exec -n bookinfo deploy/productpage-v1 -- curl -s -o /dev/null -w '%{http_code}' http://reviews.bookinfo.mesh.internal:9080/reviews/1
    #       timeout: 30s
    #       retries: 5
    #       retryDelay: 6000
    #     - action: verify
    #       expect:
    #         contains: "200"

    # - name: prefer-network-failover
    #   description: Verify PreferNetwork failover routes to east when west reviews pods are scaled down
    #   clusters:
    #     - name: west
    #   setup:
    #     - action: exec
    #       command: kubectl scale deploy/reviews-v1 reviews-v2 reviews-v3 -n bookinfo --replicas=0
    #       timeout: 30s
    #     - action: wait
    #       duration: 8000
    #   teardown:
    #     - action: exec
    #       command: kubectl scale deploy/reviews-v1 reviews-v2 reviews-v3 -n bookinfo --replicas=1
    #       timeout: 60s
    #   steps:
    #     - action: exec
    #       command: kubectl exec -n bookinfo deploy/productpage-v1 -- curl -s -o /dev/null -w '%{http_code}' http://reviews.bookinfo.mesh.internal:9080/reviews/1
    #       timeout: 30s
    #       retries: 5
    #       retryDelay: 6000
    #     - action: verify
    #       expect:
    #         contains: "200"
