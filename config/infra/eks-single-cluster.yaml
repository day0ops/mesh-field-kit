apiVersion: mesh.demo/v1
kind: InfraProfile
metadata:
  name: eks-single-cluster
  description: EKS single cluster

spec:
  name: cedar
  provider: eks
  environment: aws-dev

  settings:
    nodes:
      desired: 3
      type: t3.xlarge

  clusters:
    - name: demo
      role: management
