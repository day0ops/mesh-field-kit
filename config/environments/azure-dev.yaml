apiVersion: mesh.demo/v1
kind: Environment
metadata:
  name: azure-dev
  description: Azure AKS environment

spec:
  aks:
    region: Australia East

  acme:
    email: kasun.talwatta@solo.io

  tls:
    issuer: selfsigned-issuer

  keycloak:
    scheme: https

  images:
    keycloak: australia-southeast1-docker.pkg.dev/field-engineering-apac/kasunt/keycloak:26.7.0

  domains:
    core:
      keycloak: keycloak.ambient-demo-aks.kasunt.apac.fe.solo.io
      grafana: grafana.ambient-demo-aks.kasunt.apac.fe.solo.io
      soloUi: soloui.ambient-demo-aks.kasunt.apac.fe.solo.io
    app:
      main: app.ambient-demo-aks.kasunt.apac.fe.solo.io

  # Okta tenant
  okta:
    domain: integrator-1335842.okta.com
